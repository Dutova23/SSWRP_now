import LocalContainer from "./LocalContainer.jsx"; // Импорт компонента LocalContainer из файла LocalContainer.jsx
import "./LocalContainer.css"; // Импорт файла стилей для компонента LocalContainer

export default LocalContainer; 
